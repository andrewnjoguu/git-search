import { GitDirectDirective } from './git-direct.directive';

describe('GitDirectDirective', () => {
  it('should create an instance', () => {
    const directive = new GitDirectDirective();
    expect(directive).toBeTruthy();
  });
});
